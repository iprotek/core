You are a senior Vue 3 / frontend performance engineer.

Refactor and improve ONLY the following file:

/packages/iprotek/core/resources/js/manage/keep-alive.js

This file is used as a browser keep-alive mechanism that periodically sends a /ping request to a Laravel backend behind Cloudflare.

GOALS:
1. Prevent multiple browser tabs from sending duplicate /ping requests (must enforce single active sender).
2. Improve or fix leader election logic across tabs.
3. Ensure compatibility with older browsers (do NOT rely on crypto.randomUUID()).
4. Reduce unnecessary network traffic.
5. Add protection against race conditions in localStorage-based coordination.
6. Ensure it survives:
   - tab refresh
   - tab crash
   - multiple tabs opening at once
7. Optimize for Cloudflare-friendly behavior (low spam, stable heartbeat).
8. Improve efficiency (CPU + network minimal usage).
9. Optional: use Page Visibility API to pause when tab is hidden.
10. Optional: suggest BroadcastChannel API if it improves reliability.

REQUIREMENTS:
- Keep it a single self-contained JavaScript module.
- Do NOT introduce external dependencies.
- Keep it production-ready and simple.
- Do NOT change file purpose (still a keep-alive ping system).
- Do NOT expand scope beyond this file.

OUTPUT:
1. Fully refactored keep-alive.js code
2. Short explanation of what was improved
3. List of remaining edge cases or limitations of the approach